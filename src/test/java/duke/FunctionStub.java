package duke;

import duke.functions.Functions;
import duke.storage.Storage;
import duke.storage.TaskList;

public class FunctionStub extends Functions {

    public FunctionStub(TaskList ls, Storage st) {
        super(ls, st);
    }
}
