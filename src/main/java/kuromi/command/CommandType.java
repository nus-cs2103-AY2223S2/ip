package kuromi.command;

/**
 * An enum class to easily enumerate between Command Types entered by the user.
 */
public enum CommandType {
    bye,
    list,
    find,
    mark,
    unmark,
    delete,
    todo,
    deadline,
    event,
    remind,
    mistakes,
    help
}
