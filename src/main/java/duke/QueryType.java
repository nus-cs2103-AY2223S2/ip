package duke;

/**
 * Enumeration of the different types that a user input Command can refer to.
 */
public enum QueryType {
    list,
    mark,
    unmark,
    delete,
    todo,
    deadline,
    event,
    invalid,
    exit
}
