package duke.command;

/**
 * The Add command.
 * Encapsulates the command to add tasks.
 * Acts as a superclass for each of the different types of tasks to be added.
 */
public abstract class AddCommand extends Command {
}
