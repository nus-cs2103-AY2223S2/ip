package duke.interfaces;

/**
 * InputEventListener is an interface for listening for input events that occur in the view.
 */
public interface InputEventListener {
    /**
     * Method to handle the user input.
     * @param input The user input to be handled.
     */
    void onInputEvent(String input);
}
