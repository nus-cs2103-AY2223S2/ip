package duke;

import javafx.application.Application;

/**
 * A launcher class to act as an entry point
 * to Duke.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
