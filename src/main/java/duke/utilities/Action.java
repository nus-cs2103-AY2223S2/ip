package duke.utilities;

/**
 * Enumerable representing all recognised Duke actions.
 */
public enum Action {
    list,
    mark,
    unmark,
    todo,
    deadline,
    event,
    delete,
    bye,
    dueon,
    find,
    undo
}
