package jarvis.command.subcommand;

/**
 * Subcommand representing a duration.
 */
public class DurationCommand extends SubCommand {
    public DurationCommand(Action action, String body) {
        super(action, body);
    }
}
