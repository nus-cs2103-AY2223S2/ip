package boo.datetime;

/**
 * Represents the duration of time for which the user wants to get a reminder of his tasks, for example, whether
 * it is tasks that are due in a day or tasks due in a week.
 */
public enum ReminderDuration {
    /** Possible reminder duration. */
    DAY, WEEK, MONTH;
}
